/**
 * OTA capability marker — bundles without this export still render the
 * ChannelPlayer "Kifurushi kimekwisha" gate when playbackSuppressed is set.
 * Used by {@link ./otaBootGatePolicy.js} to force OTA fetch + reload on v24.
 */
export const KIFURUSHI_KIMEKWISHA_GATE_REMOVED = true;
