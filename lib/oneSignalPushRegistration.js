/** Web / non-native — no OneSignal push registration. */
export async function collectOneSignalPushSnapshot() {
  return null;
}

export async function ensureOneSignalPushRegistration(_reason = 'manual') {
  return null;
}
